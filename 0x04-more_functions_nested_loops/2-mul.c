#include "main.h"

/**
 * mul - prints the table x9, starting with 0
 *
 * @a: first number
 * @b: second number
 * Return: result
*/

int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
