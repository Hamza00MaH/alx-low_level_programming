#include "main.h"

/**
 * main - prints the name's program
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char **argv)
{
printf("%s\n", argv[0]);
exit(EXIT_SUCCESS);
}
