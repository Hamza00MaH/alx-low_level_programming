#include <stdio.h>
/**
	 * main -our entrypoint
	 * Description:  lowercases alphabet on ASCll,
	 * Return: 0.
*/
int main(void)
{
int lwa;
for (lwa = 97; lwa <= 122; lwa++)
char ltr = 'a' + lwa;
if(ltr == 'q' || ltr == 'e')
{}
else{
putchar(lwa);
}
putchar('\n');
return (0);
}
