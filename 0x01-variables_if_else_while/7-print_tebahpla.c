#include <stdio.h>
/**
	 * main -our entrypoint
	 * Description:  lowercases alphabet on ASCll revers.sd,
	 * Return: 0.
*/
int main(void)
{
int lwa;
for (lwa = 'z'; lwa >= 'a'; lwa--)
{
putchar(lwa);
}
putchar('\n');
return (0);
}
