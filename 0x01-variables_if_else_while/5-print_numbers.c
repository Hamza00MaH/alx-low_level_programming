#include <stdio.h>
/**
	 * main -our entrypoint
	 * Description:  all single digital numbers,
	 * Return: 0.
*/
int main(void)
{
int n;
for (n = 0; n <= 9; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
