#include "main.h"
/**
	 * _abs     -our entrypoint
	 *@i:num
	 * Description: unction that computes the absolute value of an integer,
	 * Return: abs val           .
*/
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
